package androidx.camera.core.impl;

import androidx.annotation.NonNull;
import androidx.camera.core.impl.Config;

public interface h1 extends Config {
    <ValueT> void n(@NonNull Config.a<ValueT> aVar, @NonNull Config.OptionPriority optionPriority, ValueT valuet);

    <ValueT> void p(@NonNull Config.a<ValueT> aVar, ValueT valuet);
}
