package androidx.camera.video.internal.encoder;

import androidx.annotation.NonNull;
import java.util.concurrent.Executor;

public interface m {
    @NonNull
    j a(@NonNull Executor executor, @NonNull l lVar) throws InvalidConfigException;
}
