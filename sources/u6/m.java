package u6;

import com.coffeemeetsbagel.models.dto.RetryCall;
import com.coffeemeetsbagel.models.dto.RetryCallPartial;
import java.util.List;

public interface m {
    void a(RetryCallPartial retryCallPartial);

    List<RetryCall> b();

    boolean c(RetryCall retryCall);
}
