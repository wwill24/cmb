package com.coffemeetsbagel.hide_report;

import com.coffeemeetsbagel.domain.repository.ProfileRepository;
import com.coffemeetsbagel.hide_report.i;
import fj.d;
import fj.g;

public final class l implements d<b0> {
    public static b0 a(i.b bVar, ProfileRepository profileRepository) {
        return (b0) g.e(bVar.c(profileRepository));
    }
}
