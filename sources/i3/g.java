package i3;

import androidx.annotation.NonNull;
import com.bumptech.glide.load.EncodeStrategy;
import l3.c;

public interface g<T> extends a<c<T>> {
    @NonNull
    EncodeStrategy b(@NonNull e eVar);
}
