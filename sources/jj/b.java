package jj;

import am.r;
import androidx.annotation.NonNull;
import ij.l;

public class b implements l.c<r> {
    public void a(@NonNull l lVar, @NonNull r rVar) {
        lVar.k(rVar);
        int length = lVar.length();
        lVar.d(rVar);
        lVar.i(rVar, length);
        lVar.q(rVar);
    }
}
